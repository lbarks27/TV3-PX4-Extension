diff --git a/src/modules/commander/Commander.cpp b/src/modules/commander/Commander.cpp
index ead99ad3d6..00cc40b25e 100644
--- a/src/modules/commander/Commander.cpp
+++ b/src/modules/commander/Commander.cpp
@@ -1516,5 +1516,6 @@ Commander::handle_command(const vehicle_command_s &cmd)
 	case vehicle_command_s::VEHICLE_CMD_EXTERNAL_POSITION_ESTIMATE:
 	case vehicle_command_s::VEHICLE_CMD_REQUEST_CAMERA_INFORMATION:
+	case 31010: // MAV_CMD_USER_1, handled by tv3_mode when enabled
 		/* ignore commands that are handled by other parts of the system */
 		break;
 
diff --git a/src/modules/control_allocator/ControlAllocator.cpp b/src/modules/control_allocator/ControlAllocator.cpp
index 17385a117c..38c2e74db7 100644
--- a/src/modules/control_allocator/ControlAllocator.cpp
+++ b/src/modules/control_allocator/ControlAllocator.cpp
@@ -268,11 +268,15 @@ ControlAllocator::update_effectiveness_source()
 		case EffectivenessSource::SPACECRAFT_2D:
 			// spacecraft_allocation does allocation and publishes directly to actuator_motors topic
 			break;
 
 		case EffectivenessSource::SPACECRAFT_3D:
 			// spacecraft_allocation does allocation and publishes directly to actuator_motors topic
 			break;
 
+		case EffectivenessSource::TV3:
+			tmp = new ActuatorEffectivenessTV3(this);
+			break;
+
 		default:
 			PX4_ERR("Unknown airframe");
 			break;

diff --git a/src/modules/control_allocator/ControlAllocator.hpp b/src/modules/control_allocator/ControlAllocator.hpp
index 02fe81529f..2372f5436f 100644
--- a/src/modules/control_allocator/ControlAllocator.hpp
+++ b/src/modules/control_allocator/ControlAllocator.hpp
@@ -45,5 +45,6 @@
 #include <ActuatorEffectivenessUUV.hpp>
 #include <ActuatorEffectivenessHelicopter.hpp>
 #include <ActuatorEffectivenessHelicopterCoaxial.hpp>
+#include <ActuatorEffectivenessTV3.hpp>
 
 #include <ControlAllocation.hpp>
@@ -160,5 +160,6 @@ private:
 		HELICOPTER_COAXIAL = 12,
 		SPACECRAFT_2D = 13,
 		SPACECRAFT_3D = 14,
+		TV3 = 16,
 	};

diff --git a/src/modules/control_allocator/module.yaml b/src/modules/control_allocator/module.yaml
index 1790484347..2068e2c25a 100644
--- a/src/modules/control_allocator/module.yaml
+++ b/src/modules/control_allocator/module.yaml
@@ -33,6 +33,7 @@ parameters:
                 13: Rover (Mecanum)
                 14: Spacecraft 2D
                 15: Spacecraft 3D
+                16: TV3
             default: 0
 
         CA_METHOD:
@@ -356,6 +357,292 @@ parameters:
             instance_start: 0
             default: 0
 
+        # tv3 TVC
+        CA_RK_GRP_CNT:
+            description:
+                short: Total number of tv3 TVC groups
+            type: enum
+            values:
+                0: '0'
+                1: '1'
+                2: '2'
+                3: '3'
+                4: '4'
+            default: 0
+        CA_RK_REF_THR:
+            description:
+                short: TV3 reference thrust
+                long: |
+                  Reference thrust used to build the normalized TVC effectiveness matrix.
+                  At this thrust, a full-scale torque demand should correspond to a near full-scale
+                  servo deflection for the configured geometry.
+            type: float
+            decimal: 1
+            unit: N
+            min: 0.1
+            max: 10000
+            default: 20.0
+        CA_RK_MIN_THR:
+            description:
+                short: TV3 minimum thrust estimate
+                long: |
+                  Minimum thrust used when scaling TVC authority from the measured thrust.
+                  This prevents the allocator from dividing by near-zero thrust during ignition transients.
+            type: float
+            decimal: 1
+            unit: N
+            min: 0.1
+            max: 10000
+            default: 5.0
+        CA_RK_FAL_THR:
+            description:
+                short: TV3 fallback thrust
+                long: |
+                  Fallback thrust used while the load cell is not yet valid, especially during ignition pending.
+            type: float
+            decimal: 1
+            unit: N
+            min: 0.1
+            max: 10000
+            default: 10.0
+        CA_RK_BODY_M:
+            description:
+                short: TV3 static body mass
+                long: |
+                  Airframe mass that does not burn away during boost. Use this together with the motor
+                  mass and station parameters to compute the launch and in-flight center of mass.
+            type: float
+            decimal: 3
+            unit: kg
+            min: 0
+            max: 100
+            default: 0.0
+        CA_RK_BODY_CMX:
+            description:
+                short: TV3 static body COM X
+                long: Longitudinal center of mass of the non-burning airframe, referenced to the launch CG frame.
+            type: float
+            decimal: 3
+            unit: m
+            min: -10
+            max: 10
+            default: 0.0
+        CA_RK_MOT_WET:
+            description:
+                short: TV3 motor wet mass
+                long: Loaded motor mass at ignition. The Apogee F10 defaults match the built-in mass curve.
+            type: float
+            decimal: 3
+            unit: kg
+            min: 0
+            max: 10
+            default: 0.084
+        CA_RK_MOT_DRY:
+            description:
+                short: TV3 motor burnout mass
+                long: Motor mass after the Apogee F10 propellant is consumed.
+            type: float
+            decimal: 3
+            unit: kg
+            min: 0
+            max: 10
+            default: 0.043
+        CA_RK_MOT_CMX:
+            description:
+                short: TV3 motor COM X
+                long: Longitudinal motor center of mass, referenced to the launch CG frame.
+            type: float
+            decimal: 3
+            unit: m
+            min: -10
+            max: 10
+            default: -0.25
+        CA_RK_G${i}_PX:
+            description:
+                short: TV3 group ${i} X position relative to CG
+            type: float
+            decimal: 3
+            unit: m
+            num_instances: 4
+            instance_start: 0
+            min: -10
+            max: 10
+            default: -0.25
+        CA_RK_G${i}_PY:
+            description:
+                short: TV3 group ${i} Y position relative to CG
+            type: float
+            decimal: 3
+            unit: m
+            num_instances: 4
+            instance_start: 0
+            min: -10
+            max: 10
+            default: 0.0
+        CA_RK_G${i}_PZ:
+            description:
+                short: TV3 group ${i} Z position relative to CG
+            type: float
+            decimal: 3
+            unit: m
+            num_instances: 4
+            instance_start: 0
+            min: -10
+            max: 10
+            default: 0.0
+        CA_RK_G${i}_AX:
+            description:
+                short: TV3 group ${i} thrust axis X component
+                long: Only the direction is considered.
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: 1.0
+        CA_RK_G${i}_AY:
+            description:
+                short: TV3 group ${i} thrust axis Y component
+                long: Only the direction is considered.
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: 0.0
+        CA_RK_G${i}_AZ:
+            description:
+                short: TV3 group ${i} thrust axis Z component
+                long: Only the direction is considered.
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: 0.0
+        CA_RK_G${i}_PAX:
+            description:
+                short: TV3 group ${i} pitch-axis X component
+                long: Gimbal rotation axis used by the pitch servo.
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: 0.0
+        CA_RK_G${i}_PAY:
+            description:
+                short: TV3 group ${i} pitch-axis Y component
+                long: Gimbal rotation axis used by the pitch servo.
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: -1.0
+        CA_RK_G${i}_PAZ:
+            description:
+                short: TV3 group ${i} pitch-axis Z component
+                long: Gimbal rotation axis used by the pitch servo.
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: 0.0
+        CA_RK_G${i}_YAX:
+            description:
+                short: TV3 group ${i} yaw-axis X component
+                long: Gimbal rotation axis used by the yaw servo.
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: 0.0
+        CA_RK_G${i}_YAY:
+            description:
+                short: TV3 group ${i} yaw-axis Y component
+                long: Gimbal rotation axis used by the yaw servo.
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: 0.0
+        CA_RK_G${i}_YAZ:
+            description:
+                short: TV3 group ${i} yaw-axis Z component
+                long: Gimbal rotation axis used by the yaw servo.
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: -1.0
+        CA_RK_G${i}_PMAX:
+            description:
+                short: TV3 group ${i} pitch max gimbal angle
+            type: float
+            decimal: 1
+            unit: deg
+            num_instances: 4
+            instance_start: 0
+            min: 0
+            max: 30
+            default: 5.0
+        CA_RK_G${i}_YMAX:
+            description:
+                short: TV3 group ${i} yaw max gimbal angle
+            type: float
+            decimal: 1
+            unit: deg
+            num_instances: 4
+            instance_start: 0
+            min: 0
+            max: 30
+            default: 5.0
+        CA_RK_G${i}_TF:
+            description:
+                short: TV3 group ${i} thrust fraction
+                long: Fraction of total thrust routed through this group.
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: 0
+            max: 1
+            default: [1.0, 0.0, 0.0, 0.0]
+        CA_RK_G${i}_PTR:
+            description:
+                short: TV3 group ${i} pitch trim
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: 0.0
+        CA_RK_G${i}_YTR:
+            description:
+                short: TV3 group ${i} yaw trim
+            type: float
+            decimal: 2
+            num_instances: 4
+            instance_start: 0
+            min: -1
+            max: 1
+            default: 0.0
+
         # Tilts
         CA_SV_TL_COUNT:
             description:
@@ -1276,3 +1563,69 @@ mixer:
                             function: 'axisz'
                           - name: 'CA_ROTOR${i}_CT'
                             label: "Thrust\nCoefficient"
+
+            16: # TV3
+                title: 'TV3'
+                actuators:
+                  - actuator_type: 'servo'
+                    group_label: 'TVC Groups'
+                    count: 'CA_RK_GRP_CNT'
+                    item_label_prefix: 'TVC ${i}'
+                    per_item_parameters:
+                        standard:
+                            position: [ 'CA_RK_G${i}_PX', 'CA_RK_G${i}_PY', 'CA_RK_G${i}_PZ' ]
+                        extra:
+                          - name: 'CA_RK_G${i}_AX'
+                            label: 'Thrust Axis X'
+                            function: 'axisx'
+                          - name: 'CA_RK_G${i}_AY'
+                            label: 'Thrust Axis Y'
+                            function: 'axisy'
+                          - name: 'CA_RK_G${i}_AZ'
+                            label: 'Thrust Axis Z'
+                            function: 'axisz'
+                          - name: 'CA_RK_G${i}_PAX'
+                            label: 'Pitch Axis X'
+                            advanced: true
+                          - name: 'CA_RK_G${i}_PAY'
+                            label: 'Pitch Axis Y'
+                            advanced: true
+                          - name: 'CA_RK_G${i}_PAZ'
+                            label: 'Pitch Axis Z'
+                            advanced: true
+                          - name: 'CA_RK_G${i}_YAX'
+                            label: 'Yaw Axis X'
+                            advanced: true
+                          - name: 'CA_RK_G${i}_YAY'
+                            label: 'Yaw Axis Y'
+                            advanced: true
+                          - name: 'CA_RK_G${i}_YAZ'
+                            label: 'Yaw Axis Z'
+                            advanced: true
+                          - name: 'CA_RK_G${i}_PMAX'
+                            label: 'Pitch Max Angle'
+                          - name: 'CA_RK_G${i}_YMAX'
+                            label: 'Yaw Max Angle'
+                          - name: 'CA_RK_G${i}_TF'
+                            label: 'Thrust Fraction'
+                          - name: 'CA_RK_G${i}_PTR'
+                            label: 'Pitch Trim'
+                          - name: 'CA_RK_G${i}_YTR'
+                            label: 'Yaw Trim'
+                    parameters:
+                      - label: 'Reference thrust'
+                        name: CA_RK_REF_THR
+                      - label: 'Minimum thrust estimate'
+                        name: CA_RK_MIN_THR
+                      - label: 'Fallback thrust'
+                        name: CA_RK_FAL_THR
+                      - label: 'Static body mass'
+                        name: CA_RK_BODY_M
+                      - label: 'Static body COM X'
+                        name: CA_RK_BODY_CMX
+                      - label: 'Motor wet mass'
+                        name: CA_RK_MOT_WET
+                      - label: 'Motor burnout mass'
+                        name: CA_RK_MOT_DRY
+                      - label: 'Motor COM X'
+                        name: CA_RK_MOT_CMX
--- a/src/modules/control_allocator/VehicleActuatorEffectiveness/apogee_f10_mass_curve.hpp
+++ b/src/modules/control_allocator/VehicleActuatorEffectiveness/apogee_f10_mass_curve.hpp
@@ -0,0 +1,45 @@
+#pragma once
+
+#include <array>
+#include <cstddef>
+
+struct TV3MassCurvePoint {
+	float time_s;
+	float motor_mass_kg;
+};
+
+// Derived from the existing Apogee F10 thrust curve by integrating cumulative impulse
+// and consuming 41 g of propellant from an 84 g loaded motor.
+inline constexpr float kApogeeF10LoadedMassKg = 0.084f;
+inline constexpr float kApogeeF10SpentMassKg = 0.043f;
+
+inline constexpr std::array<TV3MassCurvePoint, 28> kApogeeF10MassCurve{{
+	TV3MassCurvePoint{0.000000f, 0.084000f},
+	TV3MassCurvePoint{0.015000f, 0.083886f},
+	TV3MassCurvePoint{0.077000f, 0.082982f},
+	TV3MassCurvePoint{0.201000f, 0.081283f},
+	TV3MassCurvePoint{0.310000f, 0.079886f},
+	TV3MassCurvePoint{0.464000f, 0.078108f},
+	TV3MassCurvePoint{0.573000f, 0.076994f},
+	TV3MassCurvePoint{0.789000f, 0.075024f},
+	TV3MassCurvePoint{1.068000f, 0.072774f},
+	TV3MassCurvePoint{1.393000f, 0.070455f},
+	TV3MassCurvePoint{1.718000f, 0.068379f},
+	TV3MassCurvePoint{2.166000f, 0.065852f},
+	TV3MassCurvePoint{2.677000f, 0.063196f},
+	TV3MassCurvePoint{3.311000f, 0.060011f},
+	TV3MassCurvePoint{3.683000f, 0.058209f},
+	TV3MassCurvePoint{3.791000f, 0.057671f},
+	TV3MassCurvePoint{4.101000f, 0.056059f},
+	TV3MassCurvePoint{4.658000f, 0.053188f},
+	TV3MassCurvePoint{5.168000f, 0.050649f},
+	TV3MassCurvePoint{5.725000f, 0.047950f},
+	TV3MassCurvePoint{6.112000f, 0.046125f},
+	TV3MassCurvePoint{6.329000f, 0.045112f},
+	TV3MassCurvePoint{6.499000f, 0.044363f},
+	TV3MassCurvePoint{6.685000f, 0.043699f},
+	TV3MassCurvePoint{6.778000f, 0.043433f},
+	TV3MassCurvePoint{6.917000f, 0.043151f},
+	TV3MassCurvePoint{7.025000f, 0.043037f},
+	TV3MassCurvePoint{7.130000f, 0.043000f},
+}};

diff --git a/src/modules/control_allocator/VehicleActuatorEffectiveness/CMakeLists.txt b/src/modules/control_allocator/VehicleActuatorEffectiveness/CMakeLists.txt
index 7c6f2f8e51..0000000000 100644
--- a/src/modules/control_allocator/VehicleActuatorEffectiveness/CMakeLists.txt
+++ b/src/modules/control_allocator/VehicleActuatorEffectiveness/CMakeLists.txt
@@ -24,6 +24,8 @@ px4_add_library(VehicleActuatorEffectiveness
 	ActuatorEffectivenessTiltrotorVTOL.cpp
 	ActuatorEffectivenessTiltrotorVTOL.hpp
 	ActuatorEffectivenessTailsitterVTOL.cpp
 	ActuatorEffectivenessTailsitterVTOL.hpp
 	ActuatorEffectivenessRoverAckermann.hpp
 	ActuatorEffectivenessRoverAckermann.cpp
+	ActuatorEffectivenessTV3.hpp
+	ActuatorEffectivenessTV3.cpp
 	RpmControl.hpp
 	RpmControl.cpp
 )

diff --git a/src/modules/control_allocator/VehicleActuatorEffectiveness/ActuatorEffectivenessTV3.hpp b/src/modules/control_allocator/VehicleActuatorEffectiveness/ActuatorEffectivenessTV3.hpp
new file mode 100644
index 0000000000..0000000000
--- /dev/null
+++ b/src/modules/control_allocator/VehicleActuatorEffectiveness/ActuatorEffectivenessTV3.hpp
@@ -0,0 +1,92 @@
+#pragma once
+
+#include "control_allocation/actuator_effectiveness/ActuatorEffectiveness.hpp"
+
+#include <px4_platform_common/module_params.h>
+
+class ActuatorEffectivenessTV3 : public ModuleParams, public ActuatorEffectiveness
+{
+public:
+	static constexpr int MAX_GROUPS = 4;
+
+	explicit ActuatorEffectivenessTV3(ModuleParams *parent);
+	~ActuatorEffectivenessTV3() override = default;
+
+	bool getEffectivenessMatrix(Configuration &configuration, EffectivenessUpdateReason external_update) override;
+
+	const char *name() const override { return "TV3"; }
+
+private:
+	void updateParams() override;
+
+	struct GroupParams {
+		matrix::Vector3f position{};
+		matrix::Vector3f thrust_axis{};
+		matrix::Vector3f pitch_axis{};
+		matrix::Vector3f yaw_axis{};
+		float pitch_max_rad{0.f};
+		float yaw_max_rad{0.f};
+		float thrust_fraction{0.f};
+		float pitch_trim{0.f};
+		float yaw_trim{0.f};
+	};
+
+	struct ParamHandles {
+		param_t position_x;
+		param_t position_y;
+		param_t position_z;
+		param_t thrust_axis_x;
+		param_t thrust_axis_y;
+		param_t thrust_axis_z;
+		param_t pitch_axis_x;
+		param_t pitch_axis_y;
+		param_t pitch_axis_z;
+		param_t yaw_axis_x;
+		param_t yaw_axis_y;
+		param_t yaw_axis_z;
+		param_t pitch_max;
+		param_t yaw_max;
+		param_t thrust_fraction;
+		param_t pitch_trim;
+		param_t yaw_trim;
+	};
+
+	static matrix::Vector3f normalizeOrDefault(const matrix::Vector3f &axis, const matrix::Vector3f &fallback);
+	static matrix::Vector3f computeTorque(const matrix::Vector3f &lever_arm, const matrix::Vector3f &thrust_axis,
+					      const matrix::Vector3f &gimbal_axis, float thrust_scale, float max_angle_rad);
+
+	ParamHandles _param_handles[MAX_GROUPS]{};
+	param_t _count_handle{};
+	param_t _reference_thrust_handle{};
+	param_t _minimum_thrust_handle{};
+	param_t _fallback_thrust_handle{};
+
+	GroupParams _groups[MAX_GROUPS]{};
+	int _count{0};
+	float _reference_thrust{0.f};
+	float _minimum_thrust{0.f};
+	float _fallback_thrust{0.f};
+};

diff --git a/src/modules/control_allocator/VehicleActuatorEffectiveness/ActuatorEffectivenessTV3.cpp b/src/modules/control_allocator/VehicleActuatorEffectiveness/ActuatorEffectivenessTV3.cpp
new file mode 100644
index 0000000000..0000000000
--- /dev/null
+++ b/src/modules/control_allocator/VehicleActuatorEffectiveness/ActuatorEffectivenessTV3.cpp
@@ -0,0 +1,165 @@
+#include "ActuatorEffectivenessTV3.hpp"
+
+#include <float.h>
+#include <cstdio>
+
+#include <lib/mathlib/mathlib.h>
+#include <px4_platform_common/log.h>
+
+using namespace matrix;
+
+ActuatorEffectivenessTV3::ActuatorEffectivenessTV3(ModuleParams *parent)
+	: ModuleParams(parent)
+{
+	for (int i = 0; i < MAX_GROUPS; ++i) {
+		char buffer[17];
+
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_PX", i);
+		_param_handles[i].position_x = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_PY", i);
+		_param_handles[i].position_y = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_PZ", i);
+		_param_handles[i].position_z = param_find(buffer);
+
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_AX", i);
+		_param_handles[i].thrust_axis_x = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_AY", i);
+		_param_handles[i].thrust_axis_y = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_AZ", i);
+		_param_handles[i].thrust_axis_z = param_find(buffer);
+
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_PAX", i);
+		_param_handles[i].pitch_axis_x = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_PAY", i);
+		_param_handles[i].pitch_axis_y = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_PAZ", i);
+		_param_handles[i].pitch_axis_z = param_find(buffer);
+
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_YAX", i);
+		_param_handles[i].yaw_axis_x = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_YAY", i);
+		_param_handles[i].yaw_axis_y = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_YAZ", i);
+		_param_handles[i].yaw_axis_z = param_find(buffer);
+
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_PMAX", i);
+		_param_handles[i].pitch_max = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_YMAX", i);
+		_param_handles[i].yaw_max = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_TF", i);
+		_param_handles[i].thrust_fraction = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_PTR", i);
+		_param_handles[i].pitch_trim = param_find(buffer);
+		snprintf(buffer, sizeof(buffer), "CA_RK_G%u_YTR", i);
+		_param_handles[i].yaw_trim = param_find(buffer);
+	}
+
+	_count_handle = param_find("CA_RK_GRP_CNT");
+	_reference_thrust_handle = param_find("CA_RK_REF_THR");
+	_minimum_thrust_handle = param_find("CA_RK_MIN_THR");
+	_fallback_thrust_handle = param_find("CA_RK_FAL_THR");
+
+	updateParams();
+}
+
+void ActuatorEffectivenessTV3::updateParams()
+{
+	ModuleParams::updateParams();
+
+	int32_t count = 0;
+
+	if (param_get(_count_handle, &count) != 0) {
+		PX4_ERR("param_get failed");
+		return;
+	}
+
+	if (count < 0) {
+		count = 0;
+	}
+
+	if (count > MAX_GROUPS) {
+		count = MAX_GROUPS;
+	}
+
+	_count = count;
+
+	param_get(_reference_thrust_handle, &_reference_thrust);
+	param_get(_minimum_thrust_handle, &_minimum_thrust);
+	param_get(_fallback_thrust_handle, &_fallback_thrust);
+
+	for (int i = 0; i < _count; ++i) {
+		GroupParams &group = _groups[i];
+
+		param_get(_param_handles[i].position_x, &group.position(0));
+		param_get(_param_handles[i].position_y, &group.position(1));
+		param_get(_param_handles[i].position_z, &group.position(2));
+
+		param_get(_param_handles[i].thrust_axis_x, &group.thrust_axis(0));
+		param_get(_param_handles[i].thrust_axis_y, &group.thrust_axis(1));
+		param_get(_param_handles[i].thrust_axis_z, &group.thrust_axis(2));
+
+		param_get(_param_handles[i].pitch_axis_x, &group.pitch_axis(0));
+		param_get(_param_handles[i].pitch_axis_y, &group.pitch_axis(1));
+		param_get(_param_handles[i].pitch_axis_z, &group.pitch_axis(2));
+
+		param_get(_param_handles[i].yaw_axis_x, &group.yaw_axis(0));
+		param_get(_param_handles[i].yaw_axis_y, &group.yaw_axis(1));
+		param_get(_param_handles[i].yaw_axis_z, &group.yaw_axis(2));
+
+		float pitch_max_deg = 0.f;
+		float yaw_max_deg = 0.f;
+		param_get(_param_handles[i].pitch_max, &pitch_max_deg);
+		param_get(_param_handles[i].yaw_max, &yaw_max_deg);
+		group.pitch_max_rad = math::radians(pitch_max_deg);
+		group.yaw_max_rad = math::radians(yaw_max_deg);
+
+		param_get(_param_handles[i].thrust_fraction, &group.thrust_fraction);
+		param_get(_param_handles[i].pitch_trim, &group.pitch_trim);
+		param_get(_param_handles[i].yaw_trim, &group.yaw_trim);
+	}
+}
+
+Vector3f ActuatorEffectivenessTV3::normalizeOrDefault(const Vector3f &axis, const Vector3f &fallback)
+{
+	const float norm = axis.norm();
+
+	if (norm > FLT_EPSILON) {
+		return axis / norm;
+	}
+
+	return fallback;
+}
+
+Vector3f ActuatorEffectivenessTV3::computeTorque(const Vector3f &lever_arm, const Vector3f &thrust_axis,
+		const Vector3f &gimbal_axis, float thrust_scale, float max_angle_rad)
+{
+	// Small-angle TVC linearization: delta thrust direction ~= gimbal_axis x thrust_axis.
+	return thrust_scale * max_angle_rad * lever_arm.cross(gimbal_axis.cross(thrust_axis));
+}
+
+bool ActuatorEffectivenessTV3::getEffectivenessMatrix(Configuration &configuration,
+		EffectivenessUpdateReason external_update)
+{
+	if (external_update == EffectivenessUpdateReason::NO_EXTERNAL_UPDATE) {
+		return false;
+	}
+
+	updateParams();
+
+	if (_count <= 0) {
+		PX4_ERR("No tv3 TVC groups configured");
+		return false;
+	}
+
+	configuration.selected_matrix = 0;
+
+	for (int i = 0; i < _count; ++i) {
+		const GroupParams &group = _groups[i];
+		const Vector3f thrust_axis = normalizeOrDefault(group.thrust_axis, Vector3f{1.f, 0.f, 0.f});
+		const Vector3f pitch_axis = normalizeOrDefault(group.pitch_axis, Vector3f{0.f, -1.f, 0.f});
+		const Vector3f yaw_axis = normalizeOrDefault(group.yaw_axis, Vector3f{0.f, 0.f, -1.f});
+		const float thrust_scale = _reference_thrust * math::constrain(group.thrust_fraction, 0.f, 1.f);
+
+		int pitch_idx = configuration.addActuator(ActuatorType::SERVOS,
+				computeTorque(group.position, thrust_axis, pitch_axis, thrust_scale, group.pitch_max_rad), Vector3f{});
+
+		if (pitch_idx >= 0) {
+			configuration.trim[configuration.selected_matrix](pitch_idx) = group.pitch_trim;
+		}
+
+		int yaw_idx = configuration.addActuator(ActuatorType::SERVOS,
+				computeTorque(group.position, thrust_axis, yaw_axis, thrust_scale, group.yaw_max_rad), Vector3f{});
+
+		if (yaw_idx >= 0) {
+			configuration.trim[configuration.selected_matrix](yaw_idx) = group.yaw_trim;
+		}
+	}
+
+	return true;
+}

diff --git a/ROMFS/px4fmu_common/init.d-posix/CMakeLists.txt b/ROMFS/px4fmu_common/init.d-posix/CMakeLists.txt
index 7d7ed1d8a4..0000000000 100644
--- a/ROMFS/px4fmu_common/init.d-posix/CMakeLists.txt
+++ b/ROMFS/px4fmu_common/init.d-posix/CMakeLists.txt
@@ -1 +1,16 @@
-add_subdirectory(airframes)
+add_subdirectory(airframes)
+
+px4_add_romfs_files(
+	rcS
+	rc.replay
+	rc.tv3_defaults
+
+	px4-rc.simulator
+	px4-rc.jmavsim
+	px4-rc.sihsim
+	px4-rc.mavlink
+	px4-rc.mavlinksim
+)
